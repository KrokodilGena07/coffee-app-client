import React, {FC} from 'react';
import {OrderList} from '@/modules/orderList';

const Orders: FC = () => {
    return (
        <div>
            <OrderList/>
        </div>
    );
};

export default Orders;