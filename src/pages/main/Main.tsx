import React, {FC} from 'react';
import {OrderPanel} from '@/modules/orderPanel';

const Main: FC = () => {
    return (
        <div>
            <OrderPanel/>
        </div>
    );
};

export default Main;